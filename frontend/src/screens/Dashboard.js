import getOrder from '../api';

const OrderScreen = {
    after_render: async () => {

    },
    rend: async () => {
      
      //get purchase done by user info 
      
    //show the data from mongodb user 
    return `<div></div>`
      

    }
}

export default OrderScreen;