const { getCartItems } = require("../localStorage");

const convertCartToOrder = () =>{
    const orderItems = getCartItems() ;
};

const PlaceOrderScreen = {
    after_render :() =>{

    },
    rend : () => {}
}